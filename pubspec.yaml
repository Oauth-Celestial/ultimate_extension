name: ultimate_extension
description: "Ultimate_extension enhances Dart collections and objects with utilities for advanced data manipulation and simpler coding."
version: 0.0.4
repository: https://github.com/Oauth-Celestial/ultimate_extension

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
